describe('zoneType', function() {
	beforeEach(module('app'));

	describe('getRiskColor', function (){
		it('should be green if risk value below 1', inject(function (zoneTypeSvc){
		/*	var zoneType = 
			var user = new mvUser();
			user.roles = ['not admin'];
			expect(user.isAdmin()).to.be.falsey;*/
			expect(true).to.be.true;
		}));

	});
});